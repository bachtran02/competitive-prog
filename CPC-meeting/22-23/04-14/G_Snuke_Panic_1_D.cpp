#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

struct s {
    int size, t, x;
};

int main()
{
    ios::sync_with_stdio(false); cin.tie(nullptr);

    int n; cin >> n;
    vector<s> arr(n);

    for (auto &i : arr)
        cin >> i.t >> i.x >> i.size;

    


    return 0;
}